import React from 'react';
import {View, StyleSheet} from 'react-native';

const Note = () => {
    return (
        <View>
            
        </View>
    );
}

const styles = StyleSheet.create({})

export default Note;
